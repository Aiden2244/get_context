TEST CASE 2a
    This test case should be run with one command line argument in the case2 directory.

    The directory structure is identical to test case 1, but now case2_sub0 is listed in .contextignore.
        • Output should be identical to CASE 1a, but case2_sub0 should be omitted